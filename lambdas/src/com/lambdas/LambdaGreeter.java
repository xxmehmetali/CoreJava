package com.lambdas;

/**
 * LambdaGreeter is an interface for lambda expressions.
 * It can not contain more than one method signitare. If it contains, then lambda expression will fail.
 *
 */

public interface LambdaGreeter {
    void perform();
}
